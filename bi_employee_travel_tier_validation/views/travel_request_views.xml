<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="search_travel_req_tier" model="ir.ui.view">
        <field name="model">travel.request</field>
        <field name="inherit_id" ref="bi_employee_travel_dev.search_travel_req"/>
        <field name="arch" type="xml">
            <filter name="active" position="after">
                <separator />
                <filter
                    name="needs_review"
                    string="Needs my Review"
                    domain="[('reviewer_ids','in',uid), ('state', 'not in', ['approve', 'post', 'done', 'cancel'])]"
                    help="My travel report to review"
                />
                <filter
                    name="tier_validated"
                    string="Validated"
                    domain="[('validated', '=', True)]"
                    help="Travel request validated and ready to be approved"
                />
            </filter>
        </field>
    </record>
</odoo>
